#!/bin/sh
#
# Simple script that returns Arch's kernel version. ASCII
#
# Author: Dimitris Gerasimou <dimgerasimou@gmail.com> http://github.com/dimgerasimou
#

kern="$(uname -r | sed -re 's/(^[0-9]*\.[0-9]*\.[0-9]*)(.*)/\1/')"

echo -e "Arch: $kern"